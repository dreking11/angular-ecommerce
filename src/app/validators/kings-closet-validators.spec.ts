import { KingsClosetValidators } from './kings-closet-validators';

describe('KingsClosetValidators', () => {
  it('should create an instance', () => {
    expect(new KingsClosetValidators()).toBeTruthy();
  });
});
